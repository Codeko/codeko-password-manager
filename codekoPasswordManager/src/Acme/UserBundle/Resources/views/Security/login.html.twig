{% extends "AcmeUserBundle::layout.html.twig" %}

{% block fos_user_content %}
    <main class="container">
        <form class="form-signin bounceIn animated" action="{{ path('login_check') }}" method="post">
            <header class="decoracionCabecera"></header>
            <section class="cuerpoFormulario">
                <h2 class="form-signin-heading"><img id="headerLogo" src="{{ asset('bundles/acmeuser/images/headerLogo.png') }}"/></h2>
                <input type="hidden" name="_csrf_token" value="{% render('AcmeUserBundle:Security:getToken') %}" />    
                <label for="inputUsuario" class="sr-only">Usuario</label>
                <input type="text" id="inputUsuario" class="form-control" name="_username" placeholder="Nombre de usuario" maxlength="30" title="Introduzca un nombre de usuario" alt = "Nombre de usuario" required>
                <label for="inputPassword" class="sr-only">Contraseña</label>
                <input type="password" id="inputPassword" class="form-control" name="_password" placeholder="Contraseña" maxlength="30" title="Introduzca una contraseña" alt = "Contraseña" required>
                <div class="checkbox"></div>
                <button class="btn btn-lg btn-success btn-block" type="submit">Acceder</button>
                <div class="nuevoUsuario">
                    <ul class="nav nav-pills">
                        <li><a href="#">Registrar nuevo usuario</a></li>
                    </ul>
                </div>
            </section>
        </form>
    </main>
    {% if error %} 
        <div id="login-error">
            {{ error.message|trans }}
        </div>
    {% endif %}
    
    <!--
    {# {% block fos_user_content %} #}
        <div id="page-wrapper">
            <div id="main">
    {# {% if error %} #}
         <div id="login-error">{#{{ error.message|trans }}#}</div>
    {#{% endif %}#}
    <form action="{#{{ path('login_check') }}#}" method="post">
        <input type="hidden" name="_csrf_token" value="{#{% render('AcmeUserBundle:Security:getToken') %}#}" />
        <div class="form-item">
            <label for="username">Usuario</label>
            <input id="username" type="text" name="_username" value="{#{{ last_username }}#}" />
        </div>
        <div class="form-item">
            <label for="password">Contraseña</label>
            <input id="password" type="password" name="_password" />
        </div>
        <input type="submit" name="login" value="Iniciar sesión" />
    </form>
    </div>
    </div>
    {#{% endblock fos_user_content %}#}
    -->
{% endblock fos_user_content %}