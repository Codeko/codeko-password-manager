## Security Routes
default:
    pattern:   /
    defaults:  { _controller: CodekoLoginPrincipalBundle:Principal:principal }

principal:
    pattern:   /principal
    defaults:  { _controller: CodekoLoginPrincipalBundle:Principal:principal }
    
profile:
    pattern:   /profile
    defaults:  { _controller: CodekoLoginPrincipalBundle:Profile:show }
    
