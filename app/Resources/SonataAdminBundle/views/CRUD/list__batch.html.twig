{% extends admin.getTemplate('base_list_field') %}

{% block field %}
    <input type="checkbox" class="opcionBorrar" name="idx[]" value="{{ admin.id(object) }}" />

    <input type="radio" name="targetId" value="{{ admin.id(object) }}" />

{% endblock %}
